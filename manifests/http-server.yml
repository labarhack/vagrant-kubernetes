---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: http-server
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: http-server
    spec:
      containers:
      - name: http-server
        image: hashicorp/http-server:latest
        args: 
          - -text=hello-world
        ports:
        - name: http
          containerPort: 5678
---
apiVersion: v1
kind: Service
metadata:
  name: echo-server
  namespace: default
spec:
  ports:
  - port: 5678
    targetPort: 5678
  selector:
    app: echo-server
